BACKEND_PORT=3334
DOMAIN=localhost
PROTOCOL=http

# Anthropic API key used for /ask and /estimate chat commands
ANTHROPIC_API_KEY=

# Legacy n8n webhook (no longer used — replaced by direct Anthropic integration)
N8NCHAT=
